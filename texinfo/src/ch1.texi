@node First Chapter 
@chapter First Chapter 
    @cindex simple
    A simple minimalistic text in chapter 1. It will be processed by @i{latex2texi}.

    @section Format Latex for Texi 

    A simple converter will convert \LaTeX\ chapters to @t{texi} chapters. 
    It will strip down the @i{images}.

    @section Mathematics 

    The inline formulae are @b{valid} for texi @math{j=j_0 \cdot \left(1-\exp \frac{x}{x_0}\right)} and
    @cindex inline
    as well as multine. 
    @fnindex f(x)
    
@ifinfo 
@cite{test} 
@end ifinfo
@iflatex
@latex 
\citep{antunes2010} 
@end latex
@end iflatex

    @section Figures 
    Figures are also supported. The @t{includegraphics} command 
    will be extracted and transformed to the @t{texi} equivalent command. 
    
@ifinfo
    @image{./src/figures/image,,,,.txt}
@end ifinfo
@ifhtml
    @image{./src/figures/image,,,,.png}
@end ifhtml

@iflatex
    @latex
    \begin{figure}
    \centering
    \includegraphics[width=\textwidth]{src/figures/image.png}
    \caption{fig:caption}
    \end{figure}
    @end latex
@end iflatex

